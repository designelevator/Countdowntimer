/*
 * The selection wheel works
 * The timer milis is doing good
 * The countdown is working great
 * The animation is displaying
 * 
 * 
 * Need to make the countdown code in the same fashion than the animation code so we can have a button feedback built into it.
 * Need to have the red - orange - green colour scheme for the countdown
 * 
 * The led addressed is the actual position of the counter -1
 * 
 * 
 * 
 */
